name: Hello World
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Say-Hello:
    runs-on: ubuntu-latest
    name: Say Hello
    steps:
      - run: echo "Hello World"
